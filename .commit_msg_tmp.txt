feat(verify): 整段原文优先核查（查到即整段采信并入库，查不到再逐点）

用户要求：教材原文/成段引文先整段去查，查到就整段判"高"直接入库，查不到再逐点查。

后端
- check.js 新增"整段原文核查"：整段一次检索（原文 + 去引号两种形态并行），
  某条可引用页面与整段的内容二元组覆盖率 ≥0.7（或反算：摘要有多少出自整段 ≥0.65，
  抗摘要截断）即认为该段文字有可引用出处 → 全部断言按"高"采信、跳过逐点评级 LLM。
- 逐句验票：整段命中的页面上必须**也有本断言自身**（覆盖率 ≥0.6、重合 ≥6）才能搭车，
  否则该断言回落逐点检索+评级（实测：改写过的一句跟着整段被判"高"，而页里没有这句）。
- 抽取 LLM 新增 searchHint 字段（现代名称改写的自然语言检索词）：历史术语按字面检索
  只能召回同话题噪音（"可燃空气 水雾"→细水雾灭火论文），改写句（"氢气在空气中燃烧生成水雾"）
  才能召回维基与化学实验页；无历史术语时留空，沿用整句/实体+关键词。
- 入库改为**按实体分组**（buildStoreCardsByEntity），不再用"首条断言的实体"当整卡标题
  ——多实体段落曾把"可燃空气"的事实存进"普里斯特利"（之后查证命中答非所问）。
- 评级 prompt 补第四条反例：证据讲的是**另一个实验/另一种现象**不算冲突。

知识库
- 新增"原文直配"作为门槛①的权威依据（source.quote_match，不伪造 official_tag）；
  门槛②单域名时，覆盖率 ≥0.75 的直配本身即可放行。
- 卡内去重（draftBuilder.dedupeFacts）：label+value 或 value 相同的只留一条
  （approveEntry 不去重，实测同一句原文在"可燃空气"词条里存了两遍）。

属性口径
- TRAD2SIMP/normZh 移入 utils/attrClassify.js 并在 contentBigrams 内生效（跨繁简比对），
  映射补约 60 个化学/教育高频繁体字（氣/霧/製/鳴/氬…）——缺字会让繁体收录页算不出覆盖率。
- pickFactSentence 支持 opts.anchor/requireAnchor；修复 storeFactsOf 未转发 anchor 的死参数
  （此前"只含数据的句子"总是胜出，把爆轰论文的"速度高達100m/sec"当成事实入库）。

前端
- 核查报告顶部显示"整段原文命中出处（原文覆盖率 xx%）"横幅；逐条断言加"整段原文直配"标记。

自测：round4_fix_check.mjs 44 项全通过；线上 A/B 场景验证通过。
